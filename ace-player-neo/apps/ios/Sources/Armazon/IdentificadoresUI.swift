// Identificadores de interfaz (b-arquitectura §2.7, I0→M4). Compila en la app y en AceNeoUITests
// (project.yml): no puede depender de nada de la app, solo `static let` y `static func`.
// Esqueleto de la fase 0.1: los identificadores llegan con los contratos de la fase 0.3.
enum IDUI {}
