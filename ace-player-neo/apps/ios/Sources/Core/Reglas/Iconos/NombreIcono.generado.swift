// GENERADO por scripts/generar-iconos.mjs desde apps/web/src/ui/icons.ts. No editar.

import Foundation

/// Los 52 iconos de ui/icons.ts, en su orden (el valor es el nombre de la web).
enum NombreIcono: String, CaseIterable, Sendable {
    case agenda, biblioteca, buscar, ajustes, play, pause, stop, vol, mute, back, full, pip
    case more, star
    case starF = "star-f"
    case copy, paste, flag, refresh, check, learn
    case chevD = "chev-d"
    case chevU = "chev-u"
    case chevL = "chev-l"
    case chevR = "chev-r"
    case tv, motor, nerd, hash, link, plus, list, clock, eye
    case eyeOff = "eye-off"
    case panel, x, pencil, trash, kbd, sol, luna, pantalla, movil, qr, externo, info
    case aviso, ayuda, senal, directo, subir
}
