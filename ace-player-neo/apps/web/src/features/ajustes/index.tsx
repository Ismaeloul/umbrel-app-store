/* Entrada de la vista Ajustes para el armazón (src/app/views.tsx). El código
   vive en src/features/settings/ (y las listas en src/features/directories/). */

export { default } from '../settings/SettingsView.tsx';
