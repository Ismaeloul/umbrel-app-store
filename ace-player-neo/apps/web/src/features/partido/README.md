Centro de partido: la ruta `?vista=partido/<id>` y `partido/canal/<hash>`.
Solo reexporta: la vista está en `../match-center/` y el selector de fuentes
en `../sources/` (ver `../match-center/README.md`).
