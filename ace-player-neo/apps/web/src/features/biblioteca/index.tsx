/* Entrada de la vista Biblioteca para el armazón (src/app/views.tsx la
   encuentra por esta ruta y la carga con React.lazy en su propio trozo). El
   código vive en src/features/library/. */

export { default } from '../library/LibraryView.tsx';
